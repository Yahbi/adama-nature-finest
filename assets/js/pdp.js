/* GENERATED by build/pdp_assets.py — do not edit by hand.
   Source of truth: 1-Brand/dossier-generator/adama-content.json
   Chrome styles are extracted from index.html at build time. */
(function(){"use strict";
var CATALOG=[{"id":"shampoo","name":"Nourishing Shampoo","cat":"Hair","size":"200 mL","price":35,"img":"../assets/img/shots/adama-shampoo-320.webp","href":"shampoo.html"},{"id":"conditioner","name":"Glossing Conditioner","cat":"Hair","size":"200 mL","price":37,"img":"../assets/img/shots/adama-conditioner-320.webp","href":"conditioner.html"},{"id":"scalp-oil","name":"Rosemary Scalp Strengthening Oil","cat":"Hair","size":"50 mL","price":39,"img":"../assets/img/shots/adama-scalp-oil-320.webp","href":"scalp-oil.html"},{"id":"body-polish","name":"Volcanic Sand Body Polish","cat":"Body","size":"200 mL","price":42,"img":"../assets/img/shots/adama-body-polish-320.webp","href":"body-polish.html"},{"id":"body-cream","name":"Restorative Body Cream","cat":"Body","size":"200 mL","price":45,"img":"../assets/img/shots/adama-body-cream-320.webp","href":"body-cream.html"},{"id":"body-oil","name":"Nourishing Body Oil","cat":"Body","size":"100 mL","price":49,"img":"../assets/img/shots/adama-body-oil-320.webp","href":"body-oil.html"},{"id":"jelly-serum","name":"Prebiotic Jelly Serum","cat":"Face","size":"30 mL","price":63,"img":"../assets/img/shots/adama-jelly-serum-320.webp","href":"jelly-serum.html"},{"id":"face-mist","name":"Hydrating Face Mist","cat":"Face","size":"100 mL","price":36,"img":"../assets/img/shots/adama-face-mist-320.webp","href":"face-mist.html"},{"id":"face-oil","name":"Replenishing Facial Oil","cat":"Face","size":"30 mL","price":85,"img":"../assets/img/shots/adama-face-oil-320.webp","href":"face-oil.html"},{"id":"grand-cru","name":"Grand Cru Facial Oil","cat":"Face","size":"30 mL","price":120,"img":"../assets/img/shots/adama-grand-cru-320.webp","href":"grand-cru.html"},{"id":"sens-cleanser","name":"Calming Cleanser","cat":"Sensitive","size":"200 mL","price":38,"img":"../assets/img/shots/adama-sens-cleanser-320.webp","href":"sens-cleanser.html"},{"id":"sens-moist","name":"Soothing Moisturiser","cat":"Sensitive","size":"50 mL","price":57,"img":"../assets/img/shots/adama-sens-moist-320.webp","href":"sens-moist.html"},{"id":"sens-cream","name":"Comforting Body Cream","cat":"Sensitive","size":"200 mL","price":44,"img":"../assets/img/shots/adama-sens-cream-320.webp","href":"sens-cream.html"},{"id":"shikakai-oil","name":"Amla & Shikakai Hair Oil","cat":"Hair","size":"100 mL","price":39,"img":"../assets/img/shots/adama-shikakai-oil-320.webp","href":"shikakai-oil.html"},{"id":"shikakai-sha","name":"Shikakai & Honey Shampoo","cat":"Hair","size":"200 mL","price":33,"img":"../assets/img/shots/adama-shikakai-sha-320.webp","href":"shikakai-sha.html"},{"id":"shikakai-mask","name":"Shikakai Strengthening Hair Mask","cat":"Hair","size":"200 mL","price":36,"img":"../assets/img/shots/adama-shikakai-mask-320.webp","href":"shikakai-mask.html"},{"id":"shikakai-pow","name":"Shikakai Cleansing Powder","cat":"Hair","size":"100 g","price":24,"img":"../assets/img/shots/adama-shikakai-pow-320.webp","href":"shikakai-pow.html"},{"id":"shampoo-bar","name":"Gentle Shampoo Bar","cat":"Hair","size":"100 g","price":21,"img":"../assets/img/shots/adama-shampoo-bar-320.webp","href":"shampoo-bar.html"},{"id":"cleansing-balm","name":"Botanical Cleansing Balm","cat":"Face","size":"100 mL","price":54,"img":"../assets/img/shots/adama-cleansing-balm-320.webp","href":"cleansing-balm.html"},{"id":"clay-mask","name":"Green Clay Purifying Mask","cat":"Face","size":"100 mL","price":38,"img":"../assets/img/shots/adama-clay-mask-320.webp","href":"clay-mask.html"},{"id":"hand-wash","name":"Botanical Hand Wash","cat":"Body","size":"200 mL","price":33,"img":"../assets/img/shots/adama-hand-wash-320.webp","href":"hand-wash.html"},{"id":"hand-balm","name":"Restorative Hand Balm","cat":"Body","size":"100 mL","price":31,"img":"../assets/img/shots/adama-hand-balm-320.webp","href":"hand-balm.html"},{"id":"rosehip","name":"Pure Rosehip Seed Oil","cat":"Simples","size":"30 mL","price":29,"img":"../assets/img/shots/adama-rosehip-320.webp","href":"rosehip.html"},{"id":"jojoba","name":"Pure Jojoba Oil","cat":"Simples","size":"100 mL","price":26,"img":"../assets/img/shots/adama-jojoba-320.webp","href":"jojoba.html"},{"id":"argan","name":"Pure Argan Oil","cat":"Simples","size":"50 mL","price":28,"img":"../assets/img/shots/adama-argan-320.webp","href":"argan.html"},{"id":"squalane","name":"Olive Squalane","cat":"Simples","size":"100 mL","price":34,"img":"../assets/img/shots/adama-squalane-320.webp","href":"squalane.html"},{"id":"rose-hydro","name":"Rose Hydrosol","cat":"Simples","size":"200 mL","price":24,"img":"../assets/img/shots/adama-rose-hydro-320.webp","href":"rose-hydro.html"},{"id":"neroli-hydro","name":"Orange-Blossom Hydrosol","cat":"Simples","size":"200 mL","price":24,"img":"../assets/img/shots/adama-neroli-hydro-320.webp","href":"neroli-hydro.html"},{"id":"ghassoul","name":"Ghassoul Clay","cat":"Simples","size":"200 g","price":19,"img":"../assets/img/shots/adama-ghassoul-320.webp","href":"ghassoul.html"},{"id":"kaolin","name":"White Kaolin","cat":"Simples","size":"200 g","price":16,"img":"../assets/img/shots/adama-kaolin-320.webp","href":"kaolin.html"},{"id":"shea","name":"Raw Shea Butter","cat":"Simples","size":"200 g","price":18,"img":"../assets/img/shots/adama-shea-320.webp","href":"shea.html"},{"id":"aloe","name":"Aloe Vera Gel","cat":"Simples","size":"200 mL","price":22,"img":"../assets/img/shots/adama-aloe-320.webp","href":"aloe.html"}];
if (location.pathname.indexOf('/products/') === -1) { CATALOG.forEach(function(p){ p.img = p.img.replace('../',''); }); }

/* ---------- cart store: same key and line shape as index.html ---------- */
var KEY = 'adama-cart';
var byId = function(id){ for (var i=0;i<CATALOG.length;i++) if (CATALOG[i].id===id) return CATALOG[i]; return null; };
var money = function(n){ return '$' + n.toFixed(0); };

function readCart(){
  try {
    var raw = JSON.parse(localStorage.getItem(KEY));
    if (!Array.isArray(raw)) return [];
    // Drop lines this page cannot price: renamed SKUs, or sets that live on the
    // home page only. A line we cannot render truthfully is not rendered.
    return raw.filter(function(l){ return l && typeof l.id === 'string' && l.q > 0; });
  } catch (e) { return []; }
}
function writeCart(lines){
  // The only operation here that can genuinely fail: quota, or Safari private mode.
  localStorage.setItem(KEY, JSON.stringify(lines));
}
/* Immutable: every mutation returns a new array. */
function withLine(lines, id, delta){
  var found = false;
  var next = lines.map(function(l){
    if (l.id !== id) return l;
    found = true; return { id: l.id, q: l.q + delta };
  }).filter(function(l){ return l.q > 0; });
  return found ? next : next.concat([{ id: id, q: Math.max(delta, 1) }]);
}
function withoutLine(lines, id){ return lines.filter(function(l){ return l.id !== id; }); }

var cart = readCart();
var subtotal = function(){ return cart.reduce(function(s,l){ var p=byId(l.id); return s + (p ? p.price*l.q : 0); }, 0); };
var count = function(){ return cart.reduce(function(s,l){ return s+l.q; }, 0); };

/* ---------- persistence, as a promise: ready for a real cart API ---------- */
function persist(next){
  return new Promise(function(resolve, reject){
    try { writeCart(next); cart = next; resolve(next); }
    catch (e) { reject(e); }
  });
}

/* ---------- cart drawer ---------- */
var $ = function(s, el){ return (el||document).querySelector(s); };
var $$ = function(s, el){ return Array.prototype.slice.call((el||document).querySelectorAll(s)); };
var scrim = $('#scrim'), cartEl = $('#cart'), liveEl = null;

function announce(msg){
  if (!liveEl){
    liveEl = document.createElement('div');
    liveEl.className = 'sr-only';
    liveEl.setAttribute('aria-live','polite');
    liveEl.setAttribute('role','status');
    document.body.appendChild(liveEl);
  }
  liveEl.textContent = '';
  setTimeout(function(){ liveEl.textContent = msg; }, 40);
}

function renderCart(){
  var items = $('#cartItems');
  if (!items) return;
  if (!cart.length){
    items.innerHTML = '<div class="cart__empty">Your cart is empty.</div>';
  } else {
    items.innerHTML = cart.map(function(l){
      var p = byId(l.id);
      if (!p) return '';
      return '<div class="citem">' +
        '<div class="citem__media"><img class="citem__shot" src="' + p.img + '" alt="" loading="lazy" decoding="async"></div>' +
        '<div><div class="citem__name">' + esc(p.name) + '</div>' +
        '<div class="citem__meta">' + esc(p.cat) + ' · ' + esc(p.size) + '</div>' +
        '<div class="qty">' +
          '<button data-q="-1" data-id="' + p.id + '" aria-label="Decrease quantity of ' + esc(p.name) + '">−</button>' +
          '<span>' + l.q + '</span>' +
          '<button data-q="1" data-id="' + p.id + '" aria-label="Increase quantity of ' + esc(p.name) + '">+</button>' +
        '</div></div>' +
        '<div class="citem__right"><span class="citem__price">' + money(p.price*l.q) + '</span>' +
        '<button class="citem__rm" data-rm="' + p.id + '" aria-label="Remove ' + esc(p.name) + ' from cart">Remove</button></div></div>';
    }).join('');
  }
  var sub = $('#cartSub'); if (sub) sub.textContent = money(subtotal());
  var n = count(), badge = $('#cartCount');
  if (badge){ badge.textContent = n; badge.classList.toggle('show', n > 0); }
  var btn = $('#cartBtn');
  if (btn) btn.setAttribute('aria-label', 'Open cart — ' + n + ' item' + (n===1?'':'s'));
  var co = $('#checkout');
  if (co){ co.style.pointerEvents = cart.length ? 'auto' : 'none'; co.style.opacity = cart.length ? '1' : '.4'; }
}
function esc(s){ return String(s).replace(/[&<>"]/g, function(c){ return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[c]; }); }

function openCart(){
  cartEl.classList.add('open'); scrim.classList.add('open');
  cartEl.setAttribute('aria-hidden','false'); document.body.style.overflow = 'hidden';
}
function closeCart(){
  cartEl.classList.remove('open'); scrim.classList.remove('open');
  cartEl.setAttribute('aria-hidden','true'); document.body.style.overflow = '';
}

/* ---------- add to cart: idle → busy → added → idle, or → error ---------- */
var REVERT_MS = 1600;
function bindAdd(btn){
  var idle = btn.textContent;
  var note = $('#addNote');
  var timer = null;
  btn.addEventListener('click', function(){
    if (btn.getAttribute('aria-busy') === 'true') return;
    clearTimeout(timer);
    btn.setAttribute('aria-busy','true');
    btn.dataset.state = 'busy';
    btn.textContent = 'Adding…';
    if (note){ note.textContent = ''; note.removeAttribute('data-tone'); }

    persist(withLine(cart, btn.dataset.add, 1)).then(function(){
      renderCart();
      btn.removeAttribute('aria-busy');
      btn.dataset.state = 'added';
      btn.textContent = 'Added to cart';
      var p = byId(btn.dataset.add);
      announce((p ? p.name : 'Item') + ' added — cart has ' + count() + ' item' + (count()===1?'':'s') + '.');
      openCart();
      timer = setTimeout(function(){ btn.dataset.state = 'idle'; btn.textContent = idle; }, REVERT_MS);
    }).catch(function(){
      btn.removeAttribute('aria-busy');
      btn.dataset.state = 'error';
      btn.textContent = 'Could not add — try again';
      if (note){ note.textContent = 'Your browser is blocking local storage, so the cart cannot be saved.'; note.setAttribute('data-tone','error'); }
      announce('Could not add to cart. Your browser is blocking local storage.');
      timer = setTimeout(function(){ btn.dataset.state = 'idle'; btn.textContent = idle; }, 4000);
    });
  });
}

/* ---------- tabs: WAI-ARIA APG ---------- */
function bindTabs(root){
  var tabs = $$('[role="tab"]', root);
  if (!tabs.length) return;
  var select = function(tab, focus){
    tabs.forEach(function(t){
      var on = t === tab;
      t.setAttribute('aria-selected', on ? 'true' : 'false');
      t.tabIndex = on ? 0 : -1;
      var panel = document.getElementById(t.getAttribute('aria-controls'));
      if (panel) panel.hidden = !on;
    });
    if (focus) tab.focus();
  };
  tabs.forEach(function(tab, i){
    tab.addEventListener('click', function(){ select(tab, false); });
    tab.addEventListener('keydown', function(e){
      var k = e.key, next = null;
      if (k === 'ArrowRight') next = tabs[(i+1) % tabs.length];
      else if (k === 'ArrowLeft') next = tabs[(i-1+tabs.length) % tabs.length];
      else if (k === 'Home') next = tabs[0];
      else if (k === 'End') next = tabs[tabs.length-1];
      if (next){ e.preventDefault(); select(next, true); }
    });
  });
}

/* ---------- focus trap for the drawer ---------- */
var FOCUSABLE = 'a[href],button:not([disabled]),input:not([disabled]),select,textarea,[tabindex]:not([tabindex="-1"])';
function watchTrap(panel){
  var restoreTo = null, keyh = null;
  var vis = function(){ return $$(FOCUSABLE, panel).filter(function(el){ return el.offsetParent !== null; }); };
  var open = panel.classList.contains('open');
  new MutationObserver(function(){
    var now = panel.classList.contains('open');
    if (now && !open){
      open = true; restoreTo = document.activeElement;
      keyh = function(e){
        if (e.key !== 'Tab') return;
        var f = vis(); if (!f.length) return;
        var first = f[0], last = f[f.length-1];
        if (e.shiftKey && document.activeElement === first){ e.preventDefault(); last.focus(); }
        else if (!e.shiftKey && document.activeElement === last){ e.preventDefault(); first.focus(); }
      };
      panel.addEventListener('keydown', keyh);
      var f = vis(); if (f[0]) f[0].focus();
    } else if (!now && open){
      open = false;
      if (keyh){ panel.removeEventListener('keydown', keyh); keyh = null; }
      if (restoreTo && restoreTo.focus) restoreTo.focus();
      restoreTo = null;
    }
  }).observe(panel, { attributes: true, attributeFilter: ['class'] });
}

/* ---------- wiring ---------- */
document.addEventListener('click', function(e){
  var q = e.target.closest('[data-q]');
  if (q){ persist(withLine(cart, q.dataset.id, +q.dataset.q)).then(renderCart).catch(function(){}); return; }
  var rm = e.target.closest('[data-rm]');
  if (rm){ persist(withoutLine(cart, rm.dataset.rm)).then(renderCart).catch(function(){}); return; }
});
$$('[data-add]').forEach(bindAdd);
bindTabs(document);
if (cartEl) watchTrap(cartEl);
var cartBtn = $('#cartBtn');
if (cartBtn) cartBtn.addEventListener('click', function(){ cartEl.classList.contains('open') ? closeCart() : openCart(); });
var cartClose = $('#cartClose');
if (cartClose) cartClose.addEventListener('click', closeCart);
if (scrim) scrim.addEventListener('click', closeCart);
addEventListener('keydown', function(e){ if (e.key === 'Escape') closeCart(); });

var checkout = $('#checkout');
if (checkout) checkout.addEventListener('click', function(e){
  e.preventDefault();
  var note = $('#coNote');
  if (!note){
    note = document.createElement('p'); note.id = 'coNote';
    note.style.cssText = 'margin:.75rem 0 0;font-size:.72rem;line-height:1.55;letter-spacing:.01em;color:var(--ink-soft)';
    checkout.insertAdjacentElement('afterend', note);
  }
  note.textContent = 'Checkout opens shortly — your cart is saved.';
  announce(note.textContent);
});

var yr = document.getElementById('year');
if (yr) yr.textContent = new Date().getFullYear();
renderCart();

})();
